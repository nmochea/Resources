metadata:
  language: v1-beta
  name: "RSA private key"
  description: "Looks for RSA private key regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "-----BEGIN RSA PRIVATE KEY-----" then
     report issue:
       severity: info
       confidence: high
       detail: "RSA private key regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
