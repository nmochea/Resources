metadata:
  language: v1-beta
  name: "gradle_signing_password"
  description: "Looks for gradle_signing_password regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "gradle[_-]?signing[_-]?password(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "gradle_signing_password regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
