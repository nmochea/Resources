metadata:
  language: v1-beta
  name: "gogs_password"
  description: "Looks for gogs_password regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "gogs[_-]?password(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "gogs_password regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
