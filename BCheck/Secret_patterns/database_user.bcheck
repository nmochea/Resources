metadata:
  language: v1-beta
  name: "database_user"
  description: "Looks for database_user regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "database[_-]?user(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "database_user regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
