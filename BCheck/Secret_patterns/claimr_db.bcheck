metadata:
  language: v1-beta
  name: "claimr_db"
  description: "Looks for claimr_db regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "claimr[_-]?db(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "claimr_db regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
