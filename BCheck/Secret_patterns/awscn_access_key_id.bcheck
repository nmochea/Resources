metadata:
  language: v1-beta
  name: "awscn_access_key_id"
  description: "Looks for awscn_access_key_id regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "awscn[_-]?access[_-]?key[_-]?id(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "awscn_access_key_id regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
