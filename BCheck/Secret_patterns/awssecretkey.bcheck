metadata:
  language: v1-beta
  name: "awssecretkey"
  description: "Looks for awssecretkey regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "awssecretkey(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "awssecretkey regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
