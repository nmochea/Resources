metadata:
  language: v1-beta
  name: "consumerkey"
  description: "Looks for consumerkey regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "consumerkey(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "consumerkey regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
