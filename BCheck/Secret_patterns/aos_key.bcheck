metadata:
  language: v1-beta
  name: "aos_key"
  description: "Looks for aos_key regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "aos[_-]?key(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "aos_key regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
