metadata:
  language: v1-beta
  name: "codeclimate_repo_token"
  description: "Looks for codeclimate_repo_token regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "codeclimate[_-]?repo[_-]?token(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "codeclimate_repo_token regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
