metadata:
  language: v1-beta
  name: "github_token"
  description: "Looks for github_token regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "github[_-]?token(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "github_token regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
