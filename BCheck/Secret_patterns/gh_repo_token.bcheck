metadata:
  language: v1-beta
  name: "gh_repo_token"
  description: "Looks for gh_repo_token regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "gh[_-]?repo[_-]?token(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "gh_repo_token regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
