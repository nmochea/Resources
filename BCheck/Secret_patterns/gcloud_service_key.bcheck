metadata:
  language: v1-beta
  name: "gcloud_service_key"
  description: "Looks for gcloud_service_key regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "gcloud[_-]?service[_-]?key(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "gcloud_service_key regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
