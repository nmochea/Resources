metadata:
  language: v1-beta
  name: "bx_username"
  description: "Looks for bx_username regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "bx[_-]?username(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "bx_username regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
