metadata:
  language: v1-beta
  name: "deploy_token"
  description: "Looks for deploy_token regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "deploy[_-]?token(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "deploy_token regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
