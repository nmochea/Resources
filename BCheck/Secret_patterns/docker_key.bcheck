metadata:
  language: v1-beta
  name: "docker_key"
  description: "Looks for docker_key regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "docker[_-]?key(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "docker_key regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
