metadata:
  language: v1-beta
  name: "github_repo"
  description: "Looks for github_repo regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "github[_-]?repo(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "github_repo regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
