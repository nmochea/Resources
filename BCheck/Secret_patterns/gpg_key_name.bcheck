metadata:
  language: v1-beta
  name: "gpg_key_name"
  description: "Looks for gpg_key_name regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "gpg[_-]?key[_-]?name(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "gpg_key_name regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
