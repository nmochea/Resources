metadata:
  language: v1-beta
  name: "doordash_auth_token"
  description: "Looks for doordash_auth_token regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "doordash[_-]?auth[_-]?token(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "doordash_auth_token regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
