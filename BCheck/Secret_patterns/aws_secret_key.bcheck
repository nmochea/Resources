metadata:
  language: v1-beta
  name: "aws_secret_key"
  description: "Looks for aws_secret_key regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "aws[_-]?secret[_-]?key(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "aws_secret_key regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
