metadata:
  language: v1-beta
  name: "AWS S3 Bucket"
  description: "Looks for AWS S3 Bucket regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "s3://[0-9a-z.-_/]+" then
     report issue:
       severity: info
       confidence: tentative
       detail: "AWS S3 Bucket regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
