metadata:
  language: v1-beta
  name: "gcp_api_key"
  description: "Looks for gcp_api_key regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "(AIza[0-9A-Za-z-_]{35})" then
     report issue:
       severity: info
       confidence: tentative
       detail: "gcp_api_key regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
