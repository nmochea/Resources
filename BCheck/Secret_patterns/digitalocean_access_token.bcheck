metadata:
  language: v1-beta
  name: "digitalocean_access_token"
  description: "Looks for digitalocean_access_token regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "digitalocean[_-]?access[_-]?token(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "digitalocean_access_token regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
