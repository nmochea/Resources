metadata:
  language: v1-beta
  name: "apple_id_password"
  description: "Looks for apple_id_password regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "apple[_-]?id[_-]?password(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "apple_id_password regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
