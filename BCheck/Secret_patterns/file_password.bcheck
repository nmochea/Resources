metadata:
  language: v1-beta
  name: "file_password"
  description: "Looks for file_password regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "file[_-]?password(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "file_password regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
