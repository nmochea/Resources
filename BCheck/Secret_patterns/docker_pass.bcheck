metadata:
  language: v1-beta
  name: "docker_pass"
  description: "Looks for docker_pass regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "docker[_-]?pass(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "docker_pass regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
