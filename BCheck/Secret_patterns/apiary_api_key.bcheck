metadata:
  language: v1-beta
  name: "apiary_api_key"
  description: "Looks for apiary_api_key regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "apiary[_-]?api[_-]?key(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "apiary_api_key regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
