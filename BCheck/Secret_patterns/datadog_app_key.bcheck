metadata:
  language: v1-beta
  name: "datadog_app_key"
  description: "Looks for datadog_app_key regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "datadog[_-]?app[_-]?key(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "datadog_app_key regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
