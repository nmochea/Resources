metadata:
  language: v1-beta
  name: "b2_app_key"
  description: "Looks for b2_app_key regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "b2[_-]?app[_-]?key(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "b2_app_key regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
