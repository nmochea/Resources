metadata:
  language: v1-beta
  name: "aws_access"
  description: "Looks for aws_access regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "aws[_-]?access(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "aws_access regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
