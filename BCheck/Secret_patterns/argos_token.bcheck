metadata:
  language: v1-beta
  name: "argos_token"
  description: "Looks for argos_token regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "argos[_-]?token(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "argos_token regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
