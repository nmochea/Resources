metadata:
  language: v1-beta
  name: "bintray_gpg_password"
  description: "Looks for bintray_gpg_password regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "bintray[_-]?gpg[_-]?password(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "bintray_gpg_password regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
