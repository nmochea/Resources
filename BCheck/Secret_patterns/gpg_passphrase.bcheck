metadata:
  language: v1-beta
  name: "gpg_passphrase"
  description: "Looks for gpg_passphrase regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "gpg[_-]?passphrase(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "gpg_passphrase regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
