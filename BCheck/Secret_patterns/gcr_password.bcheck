metadata:
  language: v1-beta
  name: "gcr_password"
  description: "Looks for gcr_password regex in page source."
  author: "@nmochea"
  tags: "nmochea", "token", "exposure"

given response then
  if {latest.response} matches "gcr[_-]?password(=| =|:| :)" then
     report issue:
       severity: info
       confidence: tentative
       detail: "gcr_password regex found in page."
       remediation: "Review and remove unnecessary exposure of keys and/or sensitive data from page source."
  end if
